
/**
 * Background Service
 * 
 * Particles have been disabled as per user request.
 * This service remains as a stub to ensure application stability.
 * The background visual is now handled entirely by CSS (Mesh Gradients).
 */

export function init() {
    console.log("Background service initialized (Particles Disabled).");
}

export function destroy() {
    // No cleanup required
}
