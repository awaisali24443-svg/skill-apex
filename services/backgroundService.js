/**
 * This service used to control the animated particle background.
 * It is now disabled as the feature has been removed.
 */

export function init(configService) {
    // This function is a no-op.
}

export function destroy() {
    // This function is a no-op.
}
